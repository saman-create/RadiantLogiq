# RadiantLogiq pages and routing

## Scope and design
Extend the approved landing-page identity: Manrope/DM Sans, ink, paper, mint and lavender; generous spacing, rounded surfaces, pill actions. Do not alter the homepage design. Preserve source content from https://app.radiantlogiq.ai/ and every linked marketing destination. The source's published claims are reproduced, not independently certified.

## Implementation sequence
- [x] Add regression tests for unknown URLs, known pages, shared navigation, and transparent 404 branding.
- [x] Introduce a shared explicit route manifest and app-level not-found branch. Emit HTML entry points only for known routes. Serve HTTP 404 in Vite development/preview and configure Firebase/Vercel without an indiscriminate SPA fallback.
- [x] Build pricing, team, partners, blog, contact/demo, product, login handoff, and legal/security layouts using the existing design tokens.
- [x] Keep old authentication and server-action forms on their original trusted origin through explicit links; never collect credentials into an unconnected form or claim an inquiry was submitted. Preserve source blog summaries without inventing full articles.
- [x] Build, run tests, check localhost status codes, inspect desktop/mobile, and document deploy behavior and backend limitations.

Result: production build succeeds; 61 tests pass across 9 suites, including HTTP status checks. Impeccable bounded finish review approved after preserving request/plan intent. Live Firebase/Vercel deployment was not performed in this code-focused pass.

## Verification
Unknown routes /random and /products/unknown must not render the homepage. Known paths, query strings, trailing slash and direct reload must resolve. Marketing navigation must work from inner pages. Static output must include every known route and a self-contained 404.html using /brand/riq-logo-dark.png. Check preview HTTP status and missing assets, input labels/focus, mobile layout, and unchanged homepage.
